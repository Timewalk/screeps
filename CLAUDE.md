# Screeps Bot

## Branches
- `main` - Placeholder (don't deploy this)
- `ClaudeBot` - Active development branch (deploy with `grunt`)
- `ClaudeBotOld` - Reference implementation from alpha3

## Workflow
```bash
git checkout ClaudeBot      # switch to working branch
# edit code in src/
grunt                       # deploy to Screeps
git commit && git push      # save to GitHub
```
